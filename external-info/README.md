Other files in this directory will be ignored by git

The things referenced here, probably by symbolic links are just for temporary context for the AI assistant

Likely projects referenced:

../splint_factory