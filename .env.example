# Splint Geo Processor Environment Configuration
# Copy this to .env.local for local development
# Copy this to .env.production for production deployment

# URL of the splint_factory server
SF_API_URL=http://localhost:3000

# API key for authenticating with splint_factory
# Get this from splint_factory admin UI at /admin/api-keys
SF_API_KEY=

# Polling interval in milliseconds (default: 3000)
POLL_INTERVAL_MS=3000

# Optional: Path to Grasshopper scripts directory
# GH_SCRIPTS_DIR=./generators

# Optional: Path to Rhino CLI
# RHINO_CLI=/path/to/rhino

# Optional: Path to RhinoCode CLI
# RHINOCODE_CLI=/path/to/rhinocode

# Optional: Path to BambuStudio CLI
# BAMBU_CLI=/path/to/bambu-studio-cli

# Optional: Dry run mode (true/false)
# DRY_RUN=false
